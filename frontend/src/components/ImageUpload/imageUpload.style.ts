import { makeStyles } from "@mui/styles";

const useStyle = makeStyles((theme) => ({

    root: {
    },
    
  container: {
    width: 300,
  },
  imageContainer:{
  width:30,
  height:40,
   
  },
  image:{
    maxWidth:'100%',
    maxHeight:'100%',
    
  }

}));

export default useStyle;
